DATABASE_URL=postgres://app:app@localhost:5432/dating_app?sslmode=disable
JWT_SECRET=change-me
REDIS_ADDR=localhost:6379

# Опционально
PORT=8080
# false - если фронт ходит на API по голому HTTP (не localhost), иначе браузер не сохранит сессию
COOKIE_SECURE=false
